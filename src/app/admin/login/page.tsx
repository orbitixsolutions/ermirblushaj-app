import LoginForm from '@/components/auth/login-form'

const LoginPage = () => {
  return (
    <section className='w-full min-h-screen p-8 flex items-center bg-custom-navy'>
      <LoginForm />
    </section>
  )
}

export default LoginPage
